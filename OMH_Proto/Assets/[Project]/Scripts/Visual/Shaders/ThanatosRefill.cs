using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ThanatosRefill : MonoBehaviour
{

    [SerializeField] private InteractibleSyringe ThanatosGenerator;

    private GameObject _Centrifugeuse;
    // Start is called before the first frame update
    void Start()
    {
    
    }

    // Update is called once per frame
    void Update()
    {
        // if
    }
}
